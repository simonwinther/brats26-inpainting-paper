% sections/05_results.tex

\section{Results}
\label{sec:results}

We report quantitative results on the deterministic held-out split
(Section~\ref{sec:exp:split}) and on the official BraTS validation set, using the
healthy-mask SSIM, PSNR, and MSE metrics (with RMSE, MAE, and MSLE as secondary
measures), together with qualitative reconstructions.
% TODO: results table -- SSIM (up), PSNR (up), MSE (down), RMSE (down), MAE (down),
% MSLE (down) on the held-out split (and the official validation leaderboard).
% TODO: qualitative figure -- voided input | reconstruction | ground truth | error map.
% Numbers to be filled in after the full training run has been evaluated.
